# Docker Compose Environment Variables
# Used by docker-compose.yml and docker-compose.prod.yml

# ============================================================================
# Gateway Backend Configuration
# ============================================================================

# Passphrase for wallet encryption
GATEWAY_PASSPHRASE=your-secure-passphrase-here

# API keys for authentication (comma-separated list for multiple keys)
# Generate with: openssl rand -hex 32
GATEWAY_API_KEYS=your-api-key-here

# ============================================================================
# Gateway Frontend Configuration (for Docker deployment only)
# ============================================================================

# API key for frontend to authenticate with backend
# Should match one of the keys in GATEWAY_API_KEYS above
GATEWAY_API_KEY=your-api-key-here

# ============================================================================
# Usage
# ============================================================================

# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Generate a secure API key:
#    openssl rand -hex 32
#
# 3. Update GATEWAY_PASSPHRASE and both API key fields with your values
#
# 4. Run Gateway:
#    pnpm start --passphrase=<your-passphrase>        # Production mode (HTTPS + API key auth)
#    pnpm start --passphrase=<your-passphrase> --dev  # Development mode (HTTP, no auth)
#
# 5. Deploy with Docker:
#    docker compose up -d                           # Development
#    docker compose -f docker-compose.prod.yml up -d  # Production
#
# Note: The .env file is automatically loaded by Gateway when it starts.
#       Environment variables in .env are only used if not already set in your shell.
