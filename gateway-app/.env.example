# Gateway Configuration

# Gateway API URL
# Default: https://localhost:15888 (production mode with self-signed certs)
# Dev mode example: http://localhost:15888 (use --dev flag on Gateway)
# Remote example: https://gateway.example.com:15888
VITE_GATEWAY_URL=https://localhost:15888

# Gateway API Key (optional, only required when Gateway has apiKeys configured)
# Leave empty if Gateway is running without API keys
# Generate a secure key with: openssl rand -hex 32
VITE_GATEWAY_API_KEY=
