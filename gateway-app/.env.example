# Gateway Configuration

# Gateway API URL
# Default: http://localhost:15888 (dev mode)
# Production example: https://gateway.example.com:15888
VITE_GATEWAY_URL=http://localhost:15888

# Gateway API Key (optional, only required in production)
# Leave empty for dev mode
# Generate a secure key with: openssl rand -hex 32
VITE_GATEWAY_API_KEY=
