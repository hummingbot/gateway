{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "chainID": { "type": "integer" },
    "nodeURL": { "type": "string" },
    "nativeCurrencySymbol": { "type": "string" },
    "minGasPrice": {
      "type": "number",
      "description": "Minimum gas price in GWEI"
    },
    "maxFeePerGas": {
      "type": "number",
      "description": "EIP-1559 maximum fee per gas in GWEI (optional, will fetch from network if not set)"
    },
    "maxPriorityFeePerGas": {
      "type": "number",
      "description": "EIP-1559 maximum priority fee per gas in GWEI (optional, will fetch from network if not set)"
    }
  },
  "required": ["chainID", "nodeURL", "nativeCurrencySymbol"],
  "additionalProperties": false
}
