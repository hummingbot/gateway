{
  "type": "object",
  "properties": {
    "apiKey": {
      "type": "string",
      "description": "Infura API key"
    },
    "useWebSocket": {
      "type": "boolean",
      "default": true,
      "description": "Enable WebSocket connections for real-time events"
    },
    "cache": {
      "type": "object",
      "description": "Cache configuration for balances, positions, and pools",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Enable caching"
        },
        "trackBalances": {
          "type": "boolean",
          "description": "Track and cache wallet balances"
        },
        "trackPositions": {
          "type": "boolean",
          "description": "Track and cache CLMM positions owned by wallets"
        },
        "trackPools": {
          "type": "boolean",
          "description": "Track and cache pools listed in conf/pools/{connector}"
        },
        "refreshInterval": {
          "type": "number",
          "description": "Refresh all cached entries every N seconds"
        },
        "maxAge": {
          "type": "number",
          "description": "Consider data stale after N seconds"
        },
        "ttl": {
          "type": "number",
          "description": "Remove unused entries after N seconds of inactivity"
        }
      },
      "required": ["enabled", "trackBalances", "trackPositions", "trackPools", "refreshInterval", "maxAge", "ttl"],
      "additionalProperties": false
    }
  },
  "required": ["apiKey"],
  "additionalProperties": false
}
