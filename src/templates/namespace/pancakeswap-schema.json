{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "slippagePct": {
      "type": "number",
      "description": "Default slippage percentage (e.g., 2 for 2%)"
    },
    "maximumHops": {
      "type": "integer",
      "description": "Maximum number of hops to consider for each swap"
    },
    "maximumSplits": {
      "type": "integer",
      "description": "Maximum number of split routes for optimal execution (e.g., 60% via V3, 40% via V2)"
    }
  },
  "additionalProperties": false,
  "required": ["slippagePct", "maximumHops", "maximumSplits"]
}
