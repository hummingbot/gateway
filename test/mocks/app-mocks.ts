// Shared mocks for tests that use gatewayApp
// Uses the centralized shared mocks for consistency
import { setupCommonMocks } from './shared-mocks';

// Setup all common mocks
setupCommonMocks();

// Export empty object to make this a module
export {};
